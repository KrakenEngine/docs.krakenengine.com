language: ruby
rvm:
  - 2.3
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  fqdn: docs.krakenengine.com
  local_dir: _site
  on:
    branch: master

